# LEARNINGS.md — 복리 기록

## 2026-07-01 | 전세위험진단기 | CLAUDE.md §5 검증 게이트 점검(코드 미변경)
- 배운 것: 비밀은 `.env`(gitignore) + `os.getenv`/`st.secrets` 폴백으로 일원화하고 각 API 모듈은 키를 인자 주입받는 구조 → 하드코딩·히스토리 유출 0건으로 C1/C4 안정 통과. 신호등을 hex가 아닌 이모지로 처리해 컬러 표준 위반도 원천 차단.
- 다음에 바꿀 것: 브랜드 표준 컬러 `#1E3A8A`/`#3B82F6`가 UI에 실제 적용된 곳이 없음 → 향후 st.markdown/CSS 스타일링 시 표준 팔레트 명시 적용 권장. `.env` 로컬 평문 키는 화면 공유·백업 시 주의.
- 규칙 반영 여부: 미반영 (1회 — "키 주입 패턴"·"이모지 신호등" 좋은 패턴 관찰. 3회 누적 시 §5 또는 §8에 명문화 검토)

## 2026-07-01 | 전세위험진단기 | 브랜드 표준 컬러 UI 적용
- 배운 것: 교체할 hex가 0건이면 "교체"가 아니라 "신규 적용" 과제 → 실행 전 방향 1회 확인이 스피닝을 막음. 브랜드 테마는 `.streamlit/config.toml`의 `primaryColor`(#3B82F6)/`textColor`(#1E3A8A)에 표준 2색을 배치하고 보조 배경은 표준 블루의 옅은 틴트(#EFF6FF)로 파생하면, .py 코드에 hex를 넣지 않고도 §5 컬러 게이트를 위반 없이 통과함.
- 다음에 바꿀 것: `.streamlit/config.toml`은 비밀이 아니므로 커밋 대상. secondaryBackgroundColor 틴트 값의 공식 브랜드 가이드가 있으면 그 값으로 고정할 것. 커밋 전 `git add .streamlit/config.toml` 스테이징은 사람이 확정.
- 규칙 반영 여부: 미반영 (1회 — "테마는 config.toml, 보조색은 표준 블루 틴트 허용" 패턴. 3회 누적 시 §5 컬러 항목에 명문화 검토)
